import{createSlice}from"@reduxjs/toolkit";import{emptyMeme}from"orsys-tjs-meme";const initialState=emptyMeme,current=createSlice({name:"current",initialState:initialState,reducers:{changeMeme(state,action){delete state.id,Object.assign(state,action.payload)},resetMeme(state){delete state.id,Object.assign(state,emptyMeme)}}});export const{changeMeme:changeMeme,resetMeme:resetMeme}=current.actions;const reducerCurrent=current.reducer;export default reducerCurrent;